version: 1
services:
  - type: web
    name: ads-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: APIFY_API_TOKEN
        sync: false
      - key: META_ACCESS_TOKEN
        sync: false
